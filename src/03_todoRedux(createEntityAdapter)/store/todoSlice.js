import { createSlice, createEntityAdapter } from '@reduxjs/toolkit';

export const TodoReducer2 = createSlice({
   name : 'todoEntityAdapter',
   initialState : [],
   reducer : []
})

export const {} = TodoReducer2.actions;